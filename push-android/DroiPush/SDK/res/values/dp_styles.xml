<resources>
    <!-- Notification title style -->
    <style name="dp_notification_title_style" parent="@android:style/TextAppearance.StatusBar.EventContent.Title">
    </style>

    <!-- Notification summary style -->
    <style name="dp_notification_summary_style" parent="@android:style/TextAppearance.StatusBar.EventContent">
    </style>

    <!-- Style to show floating and translucent activity -->
    <style name="droi_dialog_activity_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimAmount">0</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
